name: non-prod workflow
on:
  workflow_call:
    inputs:
      version:
        type: string
        required: true
      environment:
        required: false
        type: string
jobs:
  non-prod:
    runs-on: nodejs
    steps: 
    - name: compute commit
      id: commit
      run: |
        short_sha=${{ inputs.version }}
        appVersion=${short_sha::9}
        echo "appVersion=$appVersion" >> $GITHUB_ENV

    - name: Check required commit statuses
      id: gate
      shell: bash
      run: |
          echo "🔍 Checking commit statuses for ${{ inputs.version }}"

          REQUIRED_CONTEXTS=(
            "DOCKER_BUILD"
            "UNIT_TESTS"
            "SCANS"
          )

          RESPONSE=$(curl -s \
            -H "Authorization: Bearer ${{ github.token }}" \
            -H "Accept: application/vnd.github+json" \
            "https://api.github.com/repos/${{ github.repository }}/commits/${{ inputs.version }}/statuses")

          FAILED=0

          for CONTEXT in "${REQUIRED_CONTEXTS[@]}"; do
            STATE=$(echo "$RESPONSE" | jq -r \
              ".[] | select(.context == \"$CONTEXT\") | .state" | head -n 1)

            if [ -z "$STATE" ]; then
              echo "❌ $CONTEXT → status missing"
              FAILED=1
            elif [ "$STATE" != "success" ]; then
              echo "❌ $CONTEXT → $STATE"
              FAILED=1
            else
              echo "✅ $CONTEXT → success"
            fi
          done

          if [ "$FAILED" -eq 1 ]; then
            echo "🚫 UAT gate failed"
            exit 1
          fi

          echo "🎉 All required checks passed"